{
  "pipeline": "default",
  "comment": "Render writes the flattened Graph connector project and DA appPackage together, so no post-render copy is needed. The feature-gated sensitivity step owns the only optional mutation.",
  "steps": [
    {
      "step": "require-empty-target"
    },
    {
      "step": "da/set-sensitivity-label",
      "when": "featureFlag('TEAMSFX_SENSITIVITY_LABEL')",
      "with": {
        "manifestPath": "appPackage/declarativeAgent.json"
      }
    }
  ]
}
