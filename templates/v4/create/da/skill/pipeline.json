{
  "pipeline": "default",
  "comment": "Render writes the local agent skill declared in declarativeAgent.json under appPackage/skills/hello-atk. require-empty-target guards the create contract; the feature-gated sensitivity step owns the only optional post-render mutation.",
  "steps": [
    {
      "step": "require-empty-target"
    },
    {
      "step": "da/set-sensitivity-label",
      "when": "featureFlag('TEAMSFX_SENSITIVITY_LABEL')",
      "with": {
        "manifestPath": "appPackage/declarativeAgent.json"
      }
    }
  ]
}
