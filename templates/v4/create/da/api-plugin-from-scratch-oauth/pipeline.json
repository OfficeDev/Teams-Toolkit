{
  "pipeline": "default",
  "comment": "Render writes the OAuth- or Microsoft-Entra-protected API plugin action pre-baked into repairDeclarativeAgent.json; apiAuth drives the MicrosoftEntra render sections. require-empty-target guards the create contract; the feature-gated sensitivity step owns the only optional post-render mutation.",
  "steps": [
    {
      "step": "require-empty-target"
    },
    {
      "step": "da/set-sensitivity-label",
      "when": "featureFlag('TEAMSFX_SENSITIVITY_LABEL')",
      "with": {
        "manifestPath": "appPackage/repairDeclarativeAgent.json"
      }
    }
  ]
}
