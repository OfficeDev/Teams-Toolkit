{
  "pipeline": "default",
  "comment": "Render writes the API plugin action pre-baked into repairDeclarativeAgent.json. require-empty-target guards the create contract; the feature-gated sensitivity step owns the only optional post-render mutation.",
  "steps": [
    {
      "step": "require-empty-target"
    },
    {
      "step": "da/set-sensitivity-label",
      "when": "featureFlag('TEAMSFX_SENSITIVITY_LABEL')",
      "with": {
        "manifestPath": "appPackage/repairDeclarativeAgent.json"
      }
    }
  ]
}
