{
  "pipeline": "default",
  "comment": "Office Nested App Auth SSO create package: pure render. The require-empty-target step guards the create contract so render writes the TypeScript add-in project without post-render injection.",
  "steps": [
    {
      "step": "require-empty-target"
    }
  ]
}
